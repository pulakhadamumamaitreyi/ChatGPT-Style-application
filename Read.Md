# ChatGPT-style App (Mock) — React + Tailwind + Node (Express)

## Overview
Simplified ChatGPT-style application (mock). Frontend is React + Tailwind. Backend is Node.js + Express serving mock JSON (no database).

## Folder structure
